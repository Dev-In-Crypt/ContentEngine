# Caddy reverse proxy — terminates TLS (auto Let's Encrypt) and forwards to the app.
# {$DOMAIN} is read from the DOMAIN env var (set in .env / docker-compose).
{$DOMAIN} {
	reverse_proxy app:8000
	# Slide/reel uploads can be a few MB; allow generous request bodies.
	request_body {
		max_size 30MB
	}

	# Response headers the app was serving none of. Each one closes something
	# specific, and the list stops where it would start breaking the page.
	header {
		# A session token lives in localStorage, so a first visit over plain
		# http is the window worth closing. Caddy already redirects; this makes
		# the browser refuse to try. One year, no preload — preload is a
		# one-way door and this host is still a temporary sslip.io name.
		Strict-Transport-Security "max-age=31536000"
		# Generated slides, uploaded logos and reels are served as bytes from
		# this same origin. Without nosniff a file that squeaks past the
		# allow-list can be re-interpreted as HTML and run here.
		X-Content-Type-Options "nosniff"
		# Nothing in this product is meant to be embedded, and Publish and
		# Delete are one click each — exactly what a transparent overlay is for.
		X-Frame-Options "DENY"
		# Post URLs carry ids; referrers leak them to every outbound link,
		# including the ones a caption points at.
		Referrer-Policy "strict-origin-when-cross-origin"
		# The app asks for none of these. Saying so stops an embedded third
		# party from asking on its behalf.
		Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"
		# Server: uvicorn tells a scanner what to try first.
		-Server
	}
	# The Content-Security-Policy is NOT here, on purpose. The application sets
	# it (backend/main.py, SecurityHeadersMiddleware) for two reasons:
	#
	#   render.yaml runs the Dockerfile behind Render's own TLS and the desktop
	#   build is a bare uvicorn, so Caddy is in front of one deployment out of
	#   three. A policy here would leave the other two with none.
	#
	#   And a header set here is invisible to the browser test suite, which runs
	#   against uvicorn directly. Set by the app, the policy is under test.
	#
	# Do not add one here as well. Two Content-Security-Policy headers are
	# INTERSECTED, so a well-meant addition silently tightens the effective
	# policy in ways nothing tests — the failure mode being a blank screen for
	# whichever resource the second header forgot.
}

# Every other hostname that must keep resolving — `www`, and an old address
# during a move — sends visitors to the canonical one above.
#
# A redirect rather than a second copy of the site, because two hostnames are
# two ORIGINS: the session token lives in localStorage, which is per-origin, so
# signing in on www would leave the apex looking logged out. The same reasoning
# applies to anything cached under an origin.
#
# The default is www.localhost on purpose: Caddy issues an internal certificate
# for localhost names instead of asking Let's Encrypt, so a deployment that
# never sets this variable gets a harmless no-op rather than a certificate
# request that fails on a loop.
{$ALIAS_DOMAINS:www.localhost} {
	redir https://{$DOMAIN}{uri} permanent
}
